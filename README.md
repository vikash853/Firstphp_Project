#  Travels — Summer Vacation Registration

A PHP + MySQL registration form for Augurs Technologies.

## Setup
1. Clone the repo
2. Create `config.php` with your DB credentials (see config.example.php)
3. Import `database.sql` in phpMyAdmin
4. Run on XAMPP / any PHP server

## Tech Stack
- PHP
- MySQL (mysqli)
- HTML/CSS
